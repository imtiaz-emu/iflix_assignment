module.exports = function (name, phoneNumber) {
  this.name = name;
  this.phoneNumber = phoneNumber;
  this.offers = [];

  this.getSubscriptionInfo = function () {
    // TODO: subscription information for every user  
  }
}